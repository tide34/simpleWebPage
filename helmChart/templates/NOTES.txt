1. Simple web application started. 

2. Chart name is {{ .Chart.Name }}. 
   Release name is {{ .Release.Name }}. 
   Namespace is {{ .Release.Namespace }}. 

3. Application based on {{ .Values.containers.repo }}/{{ .Values.containers.image }} docker image 
   and handles deployment with {{ .Values.replicaCount }} copies of pods.

4. Application can be reached over ip or URL address: 
   http://{{- .Values.externalIPs }}:{{- .Values.servicePorts.port }}
   http://{{- .Values.URL }} 

5. Application can scale up and scale down automatically  from {{ .Values.minReplicas }} to {{ .Values.maxReplicas }} 
   containers depend on CPU load. 
